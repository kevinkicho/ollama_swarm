{
  "name": "ollama-swarm",
  "version": "0.1.0",
  "private": true,
  "description": "Local multi-agent swarm system (Ollama + other providers) with Brain-as-OS for analyzing and editing codebases.",
  "bin": {
    "ollama-swarm": "./bin/ollama-swarm.mjs"
  },
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/kill-dev-ports.mjs && node scripts/dev.mjs",
    "dev:kill": "node scripts/kill-dev-ports.mjs",
    "build": "npm -w server run build && npm -w web run build",
    "start": "npm -w server run start",
    "cli": "node bin/ollama-swarm.mjs",
    "test": "npm -w shared run test && npm -w server run test",
    "run-test": "node scripts/run-test.mjs",
    "run-test:live": "node scripts/run-test.mjs --live-smoke",
    "test:shared": "npm -w shared run test",
    "typecheck": "npm -w shared run typecheck && npm -w server run typecheck && npm -w web run typecheck",
    "verify-ci": "node scripts/verify-ci.mjs",
    "setup-git-hooks": "node scripts/install-git-hooks.mjs",
    "prepare": "node scripts/install-git-hooks.mjs || echo 'Git hook setup skipped (no .git or non-fatal error)'"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  },
  "dependencies": {
    "kill-port": "^2.0.1"
  }
}
