{
  "name": "ollama-swarm",
  "version": "0.1.0",
  "private": true,
  "description": "Web app that spawns a swarm of OpenCode agents (Ollama-backed) to collaboratively analyze a GitHub repo.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm -w server run build && npm -w web run build",
    "start": "npm -w server run start"
  }
}
