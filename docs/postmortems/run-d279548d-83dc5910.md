# Postmortem: d279548d (blackboard) + 83dc5910 (council)

**Date:** 2026-07-17  
**Runs:** `d279548d-4f18-491f-bd0d-d5600fba3a25`, `83dc5910-1c3b-4056-9ebf-74660f58b45e`  
**Target repo:** ktopologymath040226 (63 HTML modules, tab expansion)

## Executive summary

| Run | Preset | Stop | Core failure mode |
|-----|--------|------|-------------------|
| d279548d | blackboard | `no-progress` | 1 commit early; planner schema-dropped todos; apply misses; 3× zero durable-progress cycles |
| 83dc5910 | council | user | applyIntegrity 32 attempts / 6 applied / 25 `search_not_found`; JSON fence + bare-key parse thrash |

## 1) d279548d — blackboard no-progress

### Timeline (from debug.jsonl)

1. Worker/auditor activity; one stream-integrity hard-truncate on agent-4 thoughts.
2. Apply-miss on grounded repair → unrepaired dry-run fail.
3. Planner seed-direct emit → later **5 schema-dropped / unbindable todos**.
4. Stuck cycle 1–3; progress gate: `openΔ` (new todos) **ignored for durable gate**.
5. Stop: `no-productive-progress: 3 cycle(s) without commits, durable met flips, or tier promotion`.

### Why so little progress

- Durable progress requires **commits**, **non-skip met flips**, or **tier promotion** (`productiveProgress.ts`). New todos alone do not reset the streak (by design — prevents audit thrash).
- After the first commit, workers failed apply and the planner stopped producing bindable todos → three empty cycles → hard stop.
- Large HTML modules + windowed prompts → invented anchors → `search_not_found`.

### hard_truncated validity

**Valid and intentional.** `finalizeAgentOutput` caps transcript fields:

- `TRANSCRIPT_THOUGHTS_HARD_MAX` = 24_000  
- `TRANSCRIPT_FINAL_TEXT_HARD_MAX` = 48_000  

Example: `hard-truncated thoughts from 30,013 (raw 30,136 → final 30,028)` means:

- Thoughts bubble was capped for **storage/UI** (not a provider stream cut).
- `raw → final` is post-strip finalText stats, not “thoughts final length”.
- **Apply/parse uses the raw worker buffer**, not the transcript-capped thoughts field.

Not a point of failure for apply success.

## 2) 83dc5910 — JSON / hunk failure storm

### applyIntegrity (summary.json)

```
attempts: 32, applied: 6
missByKind: { search_not_found: 25, search_not_unique: 1 }
repairSuccesses: 2, repairFailures: 24
```

### Failure classes (execution log)

| Class | Example | Root cause |
|-------|---------|------------|
| Fence JSON | `` Unexpected token '`' , "```json `` | gemma4 wraps hunks in markdown; unclosed fence common on long replace |
| Bare key | `{"hunks":[op":"replace"` | Missing `{"` before key — model glitch |
| Smart quotes | `Unexpected token '‘'` | Curly quotes inside HTML/prose payloads |
| Mid-JSON syntax | `Expected ',' or '}' at position N` | Truncation / unescaped content in huge replace strings |
| no hunks | `worker returned no hunks` | Empty `{"hunks":[]}` or pure think |
| search_not_found | 25× | Stale/hallucinated anchors on large HTML; concurrent multi-writer race; soft-max replace size pushes bad searches |
| Unformatted hunks in UI | Streaming partial JSON in transcript | **Display of mid-stream text**, not necessarily final parse state |

### Why repair thrash

1. Deterministic `uniqueCandidates[0]` rewrite was **opt-in** (`SWARM_APPLY_DETERMINISTIC_CANDIDATE=1`) — default off → almost every miss burned an LLM repair.
2. Soft repairs did not fix bare keys / unclosed fences on the **primary** `parseJsonEnvelope` path.
3. LLM repair often re-emitted equally bad anchors.

## 3) Patches shipped (this pass)

| Area | Change |
|------|--------|
| `shared/softJsonRepair.ts` | New: unclosed fences, bare keys, `[op":` → `[{"op":`, smart quotes, dangling `}` before `]` |
| `parseAgentJson` / `extractJson` | Soft-repair + fence strip on **primary** extract path |
| `repairJson.ts` | Delegates to shared soft repairs |
| `applyOrGroundedRepair` | Deterministic uniqueCandidates **default ON** (disable with `=0`) |
| `applyHunks` | Line-block fuzzy match (internal space collapse, multi-line) |
| `councilWorkerRunner` | Prefer schema error after repair over original fence error |
| `finalizeAgentOutput` | Message clarifies hard_truncate = transcript storage cap |

## 4) Follow-up patches (same day)

| Area | Change |
|------|--------|
| `demoteOversizedHunks` / `validateHunkPayload` | Auto-demote oversized `replace`→`replace_between`/`write`, `create`→`write` (council + blackboard workers) |
| `coercePlannerTodoItem` + salvage | string/`file` aliases, `{todos:[]}`, path inference from description |
| `buildEmergencyPlannerTodos` | Last-resort board seed from unmet criteria / repo files after schema wipe |

## 5) Residual risks (not fully eliminated)

- **Huge tab-expansion todos** can still truncate mid-JSON at the provider; demotion helps apply shape but not parse of broken streams.
- **Leading-indent-only drift** is not fuzzy-matched (only internal space collapse + trimEnd).
- Emergency micro-todos are intentionally generic — better than empty-board no-progress, not a substitute for a good planner.

## 5) Operator knobs

| Env | Default now | Effect |
|-----|-------------|--------|
| `SWARM_APPLY_DETERMINISTIC_CANDIDATE` | **on** (`1`) | `uniqueCandidates[0]` before LLM repair; set `0` to disable |

## 6) Verification

- `shared/src/softJsonRepair.test.ts` — pass  
- `server` repairJson / applyHunks / applyOrGroundedRepair / worker.parse — pass  
