# Postmortem: run `94224a3e-2d00-414c-b830-cf441beb9bbe`

**Preset:** blackboard · **Model:** `deepseek-v4-flash:cloud` · **Clone:** `kyahoofinance032926`  
**Stop reason:** `no-progress` (graceful exit, ~74s) — **not a crash**  
**Summary:** `kyahoofinance032926/logs/94224a3e/summary.json`

## What happened

The run completed contract derivation and planner-todo parsing on **attempt 2/8** (explore → emit-only retry). JSON repair was **not** the failure mode (`jsonRepairs: 0` on agent-1). Workers never ran (0 turns on agents 2–5). The run exited with:

> planner produced no actionable todos; no commits and all criteria still unmet

## Root cause

**`expectedFiles` truncation before grounding**, not parse/repair failure.

The planner emitted four paths per todo (typical panel work):

1. `src/data/sources/fetch*.js` — new module (parent dir absent from repo)
2. `src/components/panels/*Panel.jsx` — new component (parent dir absent)
3. `src/data/marketPanels.js` — **existing**
4. `src/data/panelRegistry.js` — **existing**

`lenientPreprocess` caps planner todos at **two** `expectedFiles` and previously kept the **first two** in array order. Both kept paths were **suspicious** (invented `src/data/sources/` and `src/components/panels/` trees). Grounding stripped them; each todo had zero surviving paths → **5 grounding-dropped todos**.

The valid registry paths (`marketPanels.js`, `panelRegistry.js`) were truncated away before grounding ever saw them.

## Secondary factors

- **Contract criteria** referenced `src/data/sources/` layout; this repo fetches via `functions/src/routes/` (documented in `docs/API_ENDPOINTS.md`). Contract passed because criteria with empty `expectedFiles` don't trigger hard contract-grounding rejection.
- **Explore turns** leaked DeepSeek nested `<function>…</function>` pseudo-tool XML into bubble text and thinking blocks — cosmetic/UX issue, not the stop trigger.

## Timeline (abbreviated)

| Step | Outcome |
|------|---------|
| Research pre-pass | 2583 chars web notes |
| Contract attempt 1 | Explore (pseudo-tool reads) |
| Contract attempt 2 | JSON parsed — 12 criteria |
| Planner todos attempt 1 | Explore |
| Planner todos attempt 2 | JSON parsed — 5 todos |
| Grounding | 10 suspicious paths stripped; 5 todos dropped |
| Workers | Never started |
| Stretch-goal reflection | Ran; no commits |

## Fixes shipped (2026-07-07)

1. **`prioritizeExpectedFilesSlice`** (`prompts/lenientParse.ts`) — when truncating to schema max (2), prefer shallow registry/config paths over deep `sources/` / `panels/` trees.
2. **DeepSeek `<function>` stripping** (`shared/src/extractToolCallMarkers.ts`) — removes nested function wrappers from bubble text and thoughts.
3. **Thinking panel UX** (`parseThinkingDisplay.ts`, `AgentThinking.tsx`) — prose + collapsible "Intended tool calls" list instead of raw XML.

## Re-run guidance

Re-run the same directive on `kyahoofinance032926` with current code — todos should survive grounding with at least `marketPanels.js` + `panelRegistry.js` per batch. Planner may still need better repo layout hints (`functions/src/routes/`) via seed/README; that is a prompt/contract-quality issue, not a zero-todo truncation bug.