// The function tinypkg exports. Used purely so the fixture has a
// non-trivial repo for the swarm to inspect when picking what to
// document.
export function greet(name) {
  return `Hello, ${name}!`;
}
