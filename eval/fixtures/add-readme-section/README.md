# tinypkg

A tiny utility package. The current README is intentionally minimal — it's missing a `## Usage` section that documents how to call the exported function.

## Install

`npm install tinypkg`
