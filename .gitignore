node_modules/
dist/
build/
.env
.env.local
.server-port
*.log
.DS_Store
logs/
# 2026-05-02: per-clone operational state from RunStatePersister.
# Lives next to the swarm's clone path so a server restart could
# resume mid-run (recovery deferred — write side only for now).
**/run-state.json
**/run-state.json.tmp
# Personal browser screenshots — embed copies under docs/images/ if they
# need to live in the README. The screenshots/ folder itself is local-only.
screenshots/
.claude/
.opencode/
*.tsbuildinfo
# SWE-Bench Lite dataset (~5MB, not licensed for redistribution)
eval/swe-bench/dataset/
# Lighthouse perf-test browser-profile dumps. Get accidentally created
# under perf-reports/ when a Lighthouse run keeps Chrome's user-data-dir
# alongside the report. The actual reports + perf-reports/README.md
# stay tracked; the lighthouse.NNNNNNNN profile dirs are local-only.
perf-reports/*lighthouse*/
perf-reports/C*
*.run-state.json
blackboard-state.json
