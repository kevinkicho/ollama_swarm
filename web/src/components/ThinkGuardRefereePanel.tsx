/**
 * @deprecated Think-guard LLM referee retired (2026-07-17).
 * Third-eye is deterministic stream triage (server); hard think caps only.
 * Component kept as no-op so stale imports do not break the strip.
 */
export function ThinkGuardRefereePanel(): null {
  return null;
}
